@page
@model HammadBroker.Web.Areas.Identity.Pages.Account.ExternalLoginModel
@{
	ViewData["Title"] = "تسجيل";
}

<h1>@ViewData["Title"]</h1>
<h2 id="external-login-title">ربط حساب @Model.ProviderDisplayName.</h2>
<hr />

<p id="external-login-description" class="text-info">
	لقد نجحت في المصادقة مع <strong>@Model.ProviderDisplayName</strong>.
	الرجاء إدخال عنوان بريد إلكتروني لهذا الموقع أدناه وانقر فوق الزر "تسجيل" للإنهاء
	تسجيل الدخول..
</p>

<div class="row g-0">
	<div class="col-md-12">
		<form asp-page-handler="Confirmation" asp-route-returnUrl="@Model.ReturnUrl" method="post">
			<div asp-validation-summary="ModelOnly" class="text-danger"></div>
			<div class="form-floating">
				<input asp-for="Input.Email" name="@nameof(Model.Input.Email)" class="form-control" autocomplete="email" />
				<label asp-for="Input.Email" class="form-label"></label>
				<span asp-validation-for="Input.Email" class="text-danger"></span>
			</div>
			<button type="submit" class="w-100 btn btn-lg btn-outline-primary">تسجيل</button>
		</form>
	</div>
</div>

@section Scripts {
	<partial name="~/Views/Shared/_ValidationScriptsPartial.cshtml" />
}
