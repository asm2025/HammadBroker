@using HammadBroker.Model.DTO
@model BuildingToUpdate

@{
	ViewData["Title"] = "إضافة عقار";
}

@await Component.InvokeAsync(typeof(BuildingViewComponent), new BuildingModel
{
	Building = Model,
	CanEdit = true,
	CanUpload = false
})
